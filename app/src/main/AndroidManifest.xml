<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.semana14_completa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Semana14completa">
        <activity android:name=".ClasesMetodos.ObtenerPosts"></activity>
        <activity android:name=".ClasesMetodos.ObtenerComments"></activity>
        <activity android:name=".ClasesMetodos.ObtenerAlbums"></activity>
        <activity android:name=".ClasesMetodos.ObtenerUsers"></activity>

        <activity
            android:name=".Menu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>